# Ejercicio 026 - Validaciones simples para videojuego RPG

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `personajes_rpg`.
- Se utilizaron restricciones `NOT NULL` y `CHECK` para validar nivel, puntos de vida y estado.
- Se insertaron 10 registros.
- Se utilizaron `WHERE`, `ORDER BY`, `AVG`, `COUNT`, `GROUP BY` y `LIMIT`.

## Estructura 
basico/ejercicio-026/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md