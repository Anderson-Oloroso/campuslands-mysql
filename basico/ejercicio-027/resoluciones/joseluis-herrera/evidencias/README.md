# Ejercicio 027 - Modelado de entidad para videojuego de acción y aventura

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Base de datos `campuslands_mysql`.
- Tabla `personajes` como entidad principal.
- Restricciones `PRIMARY KEY`, `NOT NULL`, `ENUM` y `CHECK`.
- 10 registros de prueba.
- Consultas con `WHERE`, `COUNT`, `AVG`, `GROUP BY`, `ORDER BY` y `LIMIT`.  

## Estructura 
basico/ejercicio-027/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md