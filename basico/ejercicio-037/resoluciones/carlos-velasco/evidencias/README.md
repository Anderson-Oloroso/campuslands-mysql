# Ejercicio 037 - ORDER BY para liga de futbol

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `equipos_futbol` con información de clasificación y rendimiento.
- Se utilizaron restricciones `PRIMARY KEY`, `UNIQUE` y `CHECK`.
- Se insertaron 10 registros coherentes.
- Se implementaron 7 consultas utilizando `ORDER BY`, filtros, cálculos y `LIMIT`.