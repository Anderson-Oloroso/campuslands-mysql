# Ejercicio 037 - ORDER BY para liga de futbol

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `equipos_liga` con restricciones para estadísticas no negativas.
- Se insertaron 10 registros coherentes.
- Se utilizaron consultas con `ORDER BY`, `ASC`, `DESC`, ordenamiento múltiple y `LIMIT`.

## Estructura 
basico/ejercicio-037/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md