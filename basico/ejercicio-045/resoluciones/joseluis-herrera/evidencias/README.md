# Evidencias

Guardar capturas o resultados que demuestren:

- Ejecución correcta de `ddl/schema.sql`.
- Creación de las tablas `categorias` y `videojuegos`.
- Inserción de las categorías y los 10 videojuegos.
- Ejecución correcta de `dql/consultas.sql`.
- Resultados de las consultas con `JOIN`.
- Resultados de filtros, ordenamientos, cálculos y ranking.

## Estructura 
basico/ejercicio-045/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md