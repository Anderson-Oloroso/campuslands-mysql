# Ejercicio 055 - COUNT y SUM para laboratorio de formulas quimicas

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `formulas_quimicas`.
- Se aplicaron restricciones para cantidades y estado.
- Se insertaron 10 registros.
- Se utilizaron `COUNT`, `SUM`, `WHERE` y `GROUP BY`.
- Se realizaron consultas generales y agrupadas por categoría y estado.


## Estructura 
basico/ejercicio-055/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md