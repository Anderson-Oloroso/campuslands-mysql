# Ejercicio 055 - COUNT y SUM para laboratorio de formulas quimicas

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `formulas_quimicas`.
- Se aplicaron `UNIQUE`, `NOT NULL` y restricciones `CHECK`.
- Se insertaron 8 fórmulas químicas.
- Se utilizaron `COUNT`, `SUM`, filtros y `GROUP BY`.