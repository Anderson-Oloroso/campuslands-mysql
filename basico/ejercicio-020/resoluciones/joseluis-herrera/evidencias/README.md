# Ejercicio 020 - SELECT para estudio de tatuajes

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `tatuajes`.
- Se aplicaron `PRIMARY KEY`, `NOT NULL`, `DEFAULT` y restricciones `CHECK`.
- Se insertaron 10 registros.
- Se utilizaron consultas `SELECT` con filtros y ordenamientos.

## Estructura 
basico/ejercicio-020/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md