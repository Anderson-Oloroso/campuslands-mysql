# Ejercicio 036 - WHERE para autos hiperdeportivos

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `autos_hiperdeportivos`.
- Se insertaron 10 registros.
- Se utilizaron `PRIMARY KEY`, `NOT NULL`, `CHECK`, `ENUM` y `UNIQUE`.
- Se practicó `WHERE` con comparaciones, `AND` y `BETWEEN`.