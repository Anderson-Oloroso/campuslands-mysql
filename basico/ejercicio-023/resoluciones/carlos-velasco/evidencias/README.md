# Ejercicio 023 - UPDATE para arquitectura 3D

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `proyectos_arquitectura_3d`.
- Se utilizaron `NOT NULL`, `ENUM` y `CHECK` para validar los datos.
- Se insertaron 10 registros.
- Se utilizaron `UPDATE`, `WHERE`, `SELECT` y `ORDER BY`.
- Las consultas finales permiten verificar los cambios realizados.