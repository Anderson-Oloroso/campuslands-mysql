# Ejercicio 050 - Subconsultas para estudio de tatuajes

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `tatuajes`.
- Se utilizaron PRIMARY KEY, NOT NULL y restricciones CHECK.
- Se insertaron 8 registros.
- Se utilizaron subconsultas escalares, `IN`, correlacionadas y `EXISTS`.

## Estructura 
intermedio/ejercicio-050/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md