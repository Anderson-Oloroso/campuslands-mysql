# Ejercicio 041 - CHECK para pingpong

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `jugadores_pingpong`.
- Se aplicaron restricciones `CHECK` para edad, ranking y estado.
- Se agregaron 10 registros con diferentes categorías y resultados.
- Se utilizaron filtros, `GROUP BY`, agregaciones, `ORDER BY`, `LIMIT` y cálculos porcentuales.