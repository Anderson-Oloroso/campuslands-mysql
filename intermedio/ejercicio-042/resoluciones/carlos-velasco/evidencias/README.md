# Ejercicio 042 - Consultas de reportes para playlist musical

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Se utilizó la base de datos `campuslands_mysql`.
- Se creó la tabla `canciones`.
- Se utilizaron `PRIMARY KEY`, `NOT NULL`, `ENUM` y restricciones `CHECK`.
- Se insertaron 10 registros.
- Las consultas utilizan filtros, ordenamientos, `COUNT`, `SUM`, `AVG`, `GROUP BY`, `HAVING` y `LIMIT`.