# Ejercicio 026 - CTE para videojuego RPG

## Ejecución

1. Ejecutar `ddl/schema.sql`.
2. Ejecutar `dml/inserts.sql`.
3. Ejecutar `dql/consultas.sql`.

## Decisiones técnicas

- Base de datos `campuslands_mysql`.
- Tabla `personajes_rpg`.
- Validaciones mediante `CHECK`.
- 10 personajes de prueba.
- Uso de CTE (`WITH`), `AVG`, `SUM`, `COUNT`, `ORDER BY` y `LIMIT`.

## Estructura 
avanzado/ejercicio-026/
└── resoluciones/
    └── joseluis-herrera/
        ├── ddl/
        │   └── schema.sql
        ├── dml/
        │   └── inserts.sql
        ├── dql/
        │   └── consultas.sql
        └── evidencias/
            └── README.md