# Evidencias - Ejercicio 053

## Evidencias requeridas

- Ejecución correcta de `ddl/schema.sql`.
- Creación de las tablas `arquitectos_3d` y `proyectos_arquitectura_3d`.
- Inserción de 8 arquitectos y 10 proyectos.
- Resultados de las consultas de filtros, relaciones, agregaciones y ranking.
- Resultado de `SHOW GRANTS` para `rol_arquitectura_lectura`.
- Resultado de `SHOW GRANTS` para `rol_arquitectura_editor`.
- Resultado de `SHOW GRANTS` para `rol_arquitectura_admin`.
- Confirmación de que los scripts fueron ejecutados en orden: DDL → DML → DQL.